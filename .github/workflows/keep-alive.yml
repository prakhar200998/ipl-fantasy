name: Keep Render Alive
on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes, 24/7
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -fsS -o /dev/null https://ipl-fantasy-5soj.onrender.com/
